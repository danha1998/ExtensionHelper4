import XCTest
@testable import ExtensionHelper4

final class ExtensionHelper4Tests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExtensionHelper4().text, "Hello, World!")
    }
}
